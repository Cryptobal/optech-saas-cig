export const metadata = {
  title: 'Dashboard | GARD',
  description: 'Panel de control principal de GARD para gestión de seguridad',
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 